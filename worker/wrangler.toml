name = "mc-coords"
main = "index.js"
compatibility_date = "2025-04-01"

# 托管前端静态文件（构建产物）
[site]
bucket = "../dist"

# D1 数据库
# 创建后填入 database_id：npx.cmd wrangler d1 create mc-coords
[[d1_databases]]
binding = "DB"
database_name = "mc-coords"
database_id = "your-database-id-here"
