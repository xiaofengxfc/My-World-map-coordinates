name = "my-world-map-coordinates"
main = "index.js"
compatibility_date = "2025-04-01"

# 静态文件由 wrangler assets 托管
# 在根目录已自动生成 wrangler.jsonc，包含 SPA fallback 配置

# D1 数据库
[[d1_databases]]
binding = "DB"
database_name = "mc-coords"
database_id = "your-database-id-here"
